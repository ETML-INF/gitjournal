Je crée ce fichier sur la branche `test_branches`. Je le commit sous le nom `commit01`, que je compte ne pas voir dans Gitjournal, qui est sur la branche main

Je modifie le .gitj pour qu'il considère la branche test_branches. `commit02`. Je compte voir apparaître les deux commits dans le jdt

j'ai mergé `test_branches`dans `main`, remis `main` dans le .gitj. `commit03` , que je compte voir avec les deux autres

j'ai supprimé la branche: le jdt reste OK. `commit04`
